#include <cstdint>

class WellnessLevels {
public:
  WellnessLevels() : steps(0), strength(0), stress(0), relations(0), school(0), confidence(0) {}

  int steps;
  int strength;
  int stress;
  int relations;
  int school;
  int confidence;

private:
};
